import React from 'react';

const Footer = () => {
    return (
        <footer className="bg-neo-bg py-10 mt-auto">
            {/* Footer content removed as requested */}
        </footer>
    );
};

export default Footer;
